# Audit Findings

Use this file as the human-readable backlog. Keep one row per finding until it is moved into the issue tracker.

| ID | Severity | Area | Repo | Environment | Role | Finding | Repro Steps | Expected | Actual | Owner | Fix Wave | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AUD-001 | P1 | API coverage | NR-Ai | local | customer / firm staff | Static API coverage found frontend calls that did not map cleanly to mounted backend routes. | Run `npm run audit:api-coverage:strict`. | Every frontend API reference maps to a mounted backend route or is explicitly parked. | Fixed in branch: strict API coverage now passes with 0 unresolved refs. | TBD | Wave 2 | Fixed |
| AUD-002 | P2 | Performance | NR-Ai | local | all roles | Production build emitted large client chunk warnings and risked eager loading of heavy document/chart dependencies. | Run `npm run build` and `npm run check`. | Route-level bundles stay below the configured warning threshold, and heavy document/chart chunks are loaded on demand rather than module-preloaded by the first HTML response. | Fixed in branch: production build has no chunk warnings, ExcelJS remains server-side/lazy, and `check-client-bundle-hygiene` now fails if the generated `index.html` preloads PDF/PDF.js/html2canvas/chart chunks. | TBD | Wave 3 | Fixed |
| AUD-003 | P3 | Build hygiene | NR-Ai | local | engineering | Production build emitted a PostCSS `from` option warning. | Run `npm run build`. | Build output is free of avoidable toolchain warnings. | Fixed in branch: the Vite 8 / plugin-react 6 / esbuild 0.28 upgrade removes the tracked PostCSS warning in the production build gate. | TBD | Wave 3 | Fixed |
| AUD-004 | P2 | Lint gate | NR-Ai | local | engineering | `npm run lint` failed because some non-browser script contexts lacked the right ESLint globals. | Run `npm run lint`. | Lint exits 0 so CI can use it as a reliable gate. | Fixed in branch: Node script/config globals and Chrome extension globals are configured, and `npm run lint` exits with no warnings. | TBD | Wave 3 | Fixed |
| AUD-005 | P3 | Code hygiene | NR-Ai | local | engineering | Lint reported a large warning backlog. | Run `npm run lint`. | Lint exits 0 with no warnings so CI can use it as a clean quality gate. | Fixed in branch: unused bindings were cleaned up, production-irrelevant React Compiler/Fast Refresh hints were removed from the lint backlog, and `npm run lint` exits cleanly. | TBD | Wave 3 | Fixed |
| AUD-006 | P1 | Release readiness | Both repos | staging | firm_owner / firm_admin | Authenticated staging smoke must cover the new client operations cockpit before the next main release. | Run `npm run smoke:credentials` from a Railway Postgres env with `DATABASE_PUBLIC_URL`, then run `npm run smoke:staging` from the app env; set `SMOKE_WORKSPACE_MUTATIONS=true` for disposable staging mutation smoke. | Health, auth session, firm clients, firm bookkeeper dashboard, firm health, Value Ops, command center, and VAT workpaper mutation paths return stable JSON shapes. | Fixed as of 2026-06-08: staging Postgres was restored, deployment `cb9d8e57-fddd-443a-b1ff-cd67b1b718c0` succeeded, authenticated staging smoke passed, and mutation smoke passed through VAT workpaper evidence upload, approval, recalculation, and return generation. | TBD | Wave 1 | Fixed |
| AUD-007 | P1 | Deployment readiness | Both repos | production | public | Railway production previously served an older commit and missed the release-branch OAuth/security routes. | Run `SMOKE_READ_ONLY=true SMOKE_EXPECTED_COMMIT=$(git rev-parse --short HEAD) npm run smoke:prod -- https://nr-ai-production.up.railway.app` and request `/api/version`. | Production target serves the release commit and exposes `/api/auth/oauth/providers` before authenticated OAuth smoke. | Fixed as of 2026-06-08: production deploy from the audit-remediation release branch completed, pre-deploy migration and schema guard passed, read-only production smoke passes liveness, readiness, version, OAuth providers, and invalid-login checks, and `/api/version` matches release `HEAD`. | TBD | Wave 1 | Fixed |

## Fix Waves

- Wave 1: P0/P1 security, login, onboarding, deploy, firm workflows.
- Wave 2: API mismatch, validation, audit logs, data integrity.
- Wave 3: UX polish, performance, observability, documentation.
- Wave 4: parked/backend-only module triage.
