# Dependencies (and any quarantined copy, e.g. node_modules.corrupt-sync-backup)
node_modules
node_modules.*/

# Local session/scratch artifacts
tmp/
outputs/
.codex-artifacts/

# Build output
dist

# Environment
.env
.env.local
.env.production

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/settings.json
.cursor
.claire/
*.swp

# Build artifacts
server/public
vite.config.ts.*
*.tar.gz
*.tsbuildinfo
node_modules/.cache

# Uploads
uploads

# Logs
*.log
logs/

# Coverage
coverage

# Replit (legacy)
.replit
replit.md
*.nix
tests/e2e/.artifacts/
docs/qa/*-benchmark-audit-local/

# E2E bootstrap runtime files (scripts/qa/bootstrap-e2e.sh)
.e2e-token
.e2e-company-id
.e2e-server.log
.e2e-server.pid

# Cloud-sync artifacts — the repo must never track these.
# (Conflict copies like "Foo 2.tsx" also fail `npm run check` via
# scripts/check-no-stray-duplicates.mjs; this keeps them out of git too.)
* 2.*
* 3.*
.fuse_hidden*
.~lock.*
*.tmp
conflicted copy*
